# Migration `20191012001239-init`

This migration has been generated by MH at 10/12/2019, 12:12:39 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Customer" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "cusType" TEXT NOT NULL DEFAULT 'B2b'  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "purchase" TEXT    REFERENCES "Purchase"(id) ON DELETE SET NULL,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  
);

CREATE TABLE "lift"."Address" (
  "customer" INTEGER    REFERENCES "Customer"(id) ON DELETE SET NULL,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "location" TEXT NOT NULL DEFAULT ''  ,
  "street" TEXT NOT NULL DEFAULT ''  ,
  "zip" TEXT NOT NULL DEFAULT ''  
);

CREATE TABLE "lift"."Product" (
  "articleNum" INTEGER NOT NULL DEFAULT 0  ,
  "color" TEXT NOT NULL DEFAULT 'RED'  ,
  "description" TEXT NOT NULL DEFAULT ''  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "price" INTEGER NOT NULL DEFAULT 0  ,
  "purchase" TEXT    REFERENCES "Purchase"(id) ON DELETE SET NULL,
  "rating" REAL NOT NULL DEFAULT 0  
);

CREATE TABLE "lift"."Purchase" (
  "coupon" TEXT    ,
  "id" TEXT NOT NULL   ,
  "purchaseId" TEXT    ,
  "revenue" INTEGER NOT NULL DEFAULT 0  ,
  "shipping" INTEGER NOT NULL DEFAULT 0  ,
  "tax" INTEGER NOT NULL DEFAULT 0  ,
  PRIMARY KEY ("id")
);
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191012001239-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,59 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model Customer {
+  id          Int      @id
+  createdAt   DateTime @default(now())
+  updatedAt   DateTime @updatedAt
+  cusType     CusType
+  addresses   Address[]
+}
+
+model Address {
+  id       Int @id
+  customer Customer
+  street   String
+  zip      String
+  location String
+}
+
+enum CusType {
+  B2b
+  B2bLight
+  B2c
+  Guest
+}
+
+model Product {
+  id          Int   @id
+  articleNum  Int
+  name        String
+  description String
+  price       Int
+  color       Color
+  rating    Float
+}
+
+enum Color {
+  RED
+  BLUE
+  GREEN
+  YELLOW
+}
+
+model Purchase {
+  id          String   @id @default(cuid())
+  purchaseId  String?
+  revenue     Int
+  tax         Int
+  shipping    Int
+  coupon      String?
+  Customer    Customer
+  products    Product[]
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191012001239-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191012001239-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
