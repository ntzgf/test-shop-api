# Migration `20191012002115-mailadress`

This migration has been generated by MH at 10/12/2019, 12:21:15 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Customer" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "customerType" TEXT NOT NULL DEFAULT 'B2b'  ,
  "email" TEXT NOT NULL DEFAULT ''  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "purchase" TEXT    REFERENCES "Purchase"(id) ON DELETE SET NULL,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  
);

INSERT INTO "new_Customer" ("createdAt","customerType","id","purchase","updatedAt") SELECT "createdAt","customerType","id","purchase","updatedAt" from "Customer"

DROP TABLE "lift"."Customer";

ALTER TABLE "lift"."new_Customer" RENAME TO "Customer";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191012001359-renamed-model..20191012002115-mailadress
--- datamodel.dml
+++ datamodel.dml
@@ -10,8 +10,9 @@
 model Customer {
   id          Int      @id
   createdAt   DateTime @default(now())
   updatedAt   DateTime @updatedAt
+  email       String
   customerType     CustomerType
   addresses   Address[]
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191012002115-mailadress)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191012002115-mailadress'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
