# Migration `20191012001359-renamed-model`

This migration has been generated by MH at 10/12/2019, 12:13:59 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Customer" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "customerType" TEXT NOT NULL DEFAULT 'B2b'  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "purchase" TEXT    REFERENCES "Purchase"(id) ON DELETE SET NULL,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  
);

CREATE TABLE "lift"."Address" (
  "country" TEXT NOT NULL DEFAULT ''  ,
  "customer" INTEGER    REFERENCES "Customer"(id) ON DELETE SET NULL,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "location" TEXT NOT NULL DEFAULT ''  ,
  "street" TEXT NOT NULL DEFAULT ''  ,
  "zip" TEXT NOT NULL DEFAULT ''  
);

CREATE TABLE "lift"."Product" (
  "articleNum" INTEGER NOT NULL DEFAULT 0  ,
  "color" TEXT NOT NULL DEFAULT 'RED'  ,
  "description" TEXT NOT NULL DEFAULT ''  ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "price" INTEGER NOT NULL DEFAULT 0  ,
  "purchase" TEXT    REFERENCES "Purchase"(id) ON DELETE SET NULL,
  "rating" REAL NOT NULL DEFAULT 0  
);

CREATE TABLE "lift"."Purchase" (
  "coupon" TEXT    ,
  "id" TEXT NOT NULL   ,
  "purchaseId" TEXT    ,
  "revenue" INTEGER NOT NULL DEFAULT 0  ,
  "shipping" INTEGER NOT NULL DEFAULT 0  ,
  "tax" INTEGER NOT NULL DEFAULT 0  ,
  PRIMARY KEY ("id")
);
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191012001239-init..20191012001359-renamed-model
--- datamodel.dml
+++ datamodel.dml
@@ -10,9 +10,9 @@
 model Customer {
   id          Int      @id
   createdAt   DateTime @default(now())
   updatedAt   DateTime @updatedAt
-  cusType     CusType
+  customerType     CustomerType
   addresses   Address[]
 }
 model Address {
@@ -20,11 +20,12 @@
   customer Customer
   street   String
   zip      String
   location String
+  country  String
 }
-enum CusType {
+enum CustomerType {
   B2b
   B2bLight
   B2c
   Guest
@@ -36,9 +37,9 @@
   name        String
   description String
   price       Int
   color       Color
-  rating    Float
+  rating      Float
 }
 enum Color {
   RED
```

## Photon Usage

You can use a specific Photon built for this migration (20191012001359-renamed-model)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191012001359-renamed-model'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
